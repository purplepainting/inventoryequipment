import React from 'react';
import { Box, Typography } from '@mui/material';

export default function TransactionDashboard() {
  return (
    <Box>
      <Typography variant="h4">Transaction Dashboard</Typography>
      <Typography variant="body1">Transaction dashboard functionality coming soon...</Typography>
    </Box>
  );
} 